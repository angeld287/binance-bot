{
  "CHANNEL_TOLERANCE_SLOPE": {
    "default": 0.05,
    "description": "Maximum allowed absolute slope difference between channel boundaries."
  },
  "CHANNEL_MIN_TOUCHES": {
    "default": 3,
    "description": "Minimum pivot touches required on each boundary."
  },
  "CHANNEL_MIN_VERTICAL_DISTANCE_PCT": {
    "default": 0.01,
    "description": "Minimum vertical separation between channel lines relative to price."
  },
  "CHANNEL_MIN_DURATION_BARS": {
    "default": 20,
    "description": "Minimum number of bars the formation must span."
  },
  "CHANNEL_CONFIDENCE_THRESHOLD": {
    "default": 1.5,
    "description": "Minimum RR or confidence score expected from the detector."
  },
  "TP_MODE": {
    "default": "opuesto_inmediato",
    "description": "Take-profit selection policy."
  },
  "SL_ENABLED": {
    "default": false,
    "description": "Toggle for stop-loss placement (kept off by default)."
  },
  "CHANNEL_FIXED_SL_PCT": {
    "default": 1.0,
    "description": "Fixed percentage distance from entry used for stop-loss placement."
  },
  "PRICE_TICK_OVERRIDE": {
    "default": null,
    "description": "Optional explicit tick size override for precision troubleshooting."
  },
  "QTY_STEP_OVERRIDE": {
    "default": null,
    "description": "Optional explicit quantity step override for precision troubleshooting."
  },
  "MIN_NOTIONAL_BUFFER_PCT": {
    "default": 0.03,
    "description": "Extra percentage buffer applied over the exchange min notional."
  },
  "CHANNEL_MAX_TRADES_PER_CHANNEL": {
    "default": 1,
    "description": "Maximum number of simultaneous trades allowed per detected channel."
  },
  "PARALLEL_CHANNEL_EMA_DISTANCE_FILTER_ENABLED": {
    "default": true,
    "description": "Toggle for rejecting entries when price is too far from the fast EMA."
  },
  "PARALLEL_CHANNEL_EMA_DISTANCE_THRESHOLD_PCT": {
    "default": 0.8,
    "description": "Maximum allowed percentage distance from price to the fast EMA before rejecting an entry."
  },
  "EMA_TransitionFilter_Enabled": {
    "default": true,
    "description": "Toggle to enable the EMA transition entry filter that avoids trades in transition zones."
  },
  "EMA_Fast_Slope_Threshold": {
    "default": 0.0005,
    "description": "Minimum absolute slope for the fast EMA required to consider there is enough momentum to trade."
  }
}
